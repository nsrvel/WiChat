# Frontend

Web client (Next.js, primary UI) will be added here **one vertical slice at a time** — not ahead of backend review.

Planned layout:

```
frontend/
  web/          # Next.js app (web + iOS PWA)
```

Desktop (Electron) and mobile (React Native) attach later; they reuse the same API as `web/`.
